module.exports.config = {
    name: '',
    version: '1.0.0',
    credit: 'DungDz',
    description: 'Noti!',
    usage: ''
}

module.exports.run = function (api, event, client) {
    // Hàm được thực thi khi có 1 event bất kì xảy ra
}

module.exports.onload = function (api, client) {
    // Hàm được thực thi khi bot khởi chạy
}
